name: Daily Salesforce Job Radar (legacy disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement workflow
        run: |
          echo "This legacy MongoDB cloud sync workflow is intentionally disabled."
          echo "Use the Salesforce Job Radar Agent workflow instead:"
          echo ".github/workflows/salesforce-job-radar-agent.yml"
